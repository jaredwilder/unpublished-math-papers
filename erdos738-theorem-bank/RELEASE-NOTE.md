# Release note

The source packet is published verbatim alongside structured records and an independent finite audit. The release preserves the original distinction between `PROVED_IN_PACKET` and `UNPROVED_CHECKABLE_TARGET` and does not convert finite testing into formal proof or historical novelty.