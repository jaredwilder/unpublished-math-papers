# Paper source

`main.tex` is a concise manuscript describing the Round 4 computational theorem, certificate construction, independent verification, and claim scope.

Compile with any standard LaTeX installation:

```bash
pdflatex main.tex
pdflatex main.tex
```

The proof artifact is the certificate and verifier suite in the repository, not the rendered PDF.
