# Novelty clearance — Schur–Roth subsets of `Z/31Z`

## Claim searched

The exact maximum and classification of subsets `A ⊆ Z/31Z` that are simultaneously:

- standard sum-free: `(A+A) ∩ A = ∅`;
- nontrivial three-term-progression-free.

The result searched was:

- maximum cardinality `6`;
- exactly `330` extremal six-sets;
- exactly `12` dilation orbits under `(Z/31Z)^×`.

## Search families

The clearance used exact and broadened searches including:

- `"sum-free and 3-AP-free"`;
- `"simultaneously sum-free" progression-free`;
- `"sum-free" "3-term arithmetic progression" cyclic group`;
- `"Z/31Z" sum-free progression-free`;
- arXiv-restricted versions of those searches;
- OEIS searches for combined sum-free/progression-free terminology and the computed finite sequence;
- nearest-neighbor literature on `W`-avoiding sets, complete 3-AP-free sets, ordinary sum-free sets, and tri-colored sum-free sets.

## Closest literature found

The closest recent work studies complete or saturating 3-AP-free sets and general `W`-avoidance. It explicitly distinguishes 3-AP-free sets from restricted sum-free sets and focuses primarily on a single forbidden equation or on minimum sizes of maximal sets. It does not state the simultaneous standard-sum-free plus 3-AP-free extremal problem above.

Other nearby work studies:

- maximum `(k,l)`-sum-free subsets of cyclic groups;
- complete symmetric sum-free sets;
- ordinary progression-free subsets;
- tri-colored sum-free sets;
- complexity of avoiding one or multiple linear equations.

No searched source stated the exact `Z/31Z` value `6`, the count `330`, or the 12-orbit classification for this simultaneous property.

## Verdict

```text
mathematical truth: PROVED
novelty status: APPARENTLY NEW AFTER SYSTEMATIC SEARCH
safe wording: "to the best of our knowledge"
```

Novelty is not used as a premise in the theorem certificate.